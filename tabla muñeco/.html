<!DOCTYPE html>
<html>
<head>
  <title>Mover 'x' en un array</title>
</head>
<body>
  <script>
    const array = ['a', 'b', 'c', 'x', 'd', 'e', 'f']; 

    let position =  1 ; 

    
    function displayArray() {
      const result = array.map((item, index) => (index === position ? `[${item}]` : item));
      console.log(result.join(' '));
    }

    displayArray(); 

   
    function moveForward() {
      if (position < array.length - 1) {
        position++;
        displayArray();
      }
    }

   
    function moveBackward() {
      if (position > 0) {
        position--;
        displayArray();
      }
    }


    document.addEventListener('keydown', (event) => {
      if (event.key === 'ArrowRight') {
        moveForward();
      } else if (event.key === 'ArrowLeft') {
        moveBackward();
      }
    });
  </script>
</body>
</html>
